---
phase: 01-data-foundation
plan: 03
type: execute
wave: 2
depends_on:
  - "01-01"
  - "01-02"
files_modified:
  - src/ingestion/databricks_job.py
  - databricks/jobs/poll_job_config.json
  - databricks/notebooks/run_poll.py
  - tests/test_streaming.py
  - data/fallback/sample_flights.json
autonomous: false
requirements:
  - STRM-01
  - STRM-02
  - STRM-03

must_haves:
  truths:
    - "Polling job runs on schedule and writes to landing zone"
    - "DLT streaming pipeline processes new files within 60 seconds"
    - "Late-arriving data handled via watermark (not dropped silently)"
    - "Pipeline can restart from checkpoint without data loss"
    - "Fallback data available when API unavailable"
  artifacts:
    - path: "src/ingestion/databricks_job.py"
      provides: "Databricks job wrapper for polling"
      exports: ["run_poll_job"]
    - path: "databricks/jobs/poll_job_config.json"
      provides: "Databricks Workflows job configuration"
    - path: "databricks/notebooks/run_poll.py"
      provides: "Notebook for job execution"
    - path: "data/fallback/sample_flights.json"
      provides: "Cached flight data for offline demo"
    - path: "tests/test_streaming.py"
      provides: "Streaming behavior tests"
  key_links:
    - from: "databricks/notebooks/run_poll.py"
      to: "src/ingestion/poll_job.py"
      via: "import and execute"
      pattern: "poll_and_write"
    - from: "databricks/jobs/poll_job_config.json"
      to: "databricks/notebooks/run_poll.py"
      via: "notebook task reference"
      pattern: "notebook_path"

user_setup:
  - service: opensky
    why: "Flight data API for real-time positions"
    env_vars:
      - name: OPENSKY_CLIENT_ID
        source: "OpenSky Network account settings > API credentials"
      - name: OPENSKY_CLIENT_SECRET
        source: "OpenSky Network account settings > API credentials"
    dashboard_config:
      - task: "Create OAuth2 application"
        location: "https://opensky-network.org - Account Settings > Applications"
---

<objective>
Configure the streaming infrastructure: Databricks job for polling, streaming latency verification, checkpoint resilience, and fallback data for offline mode.

Purpose: Complete the real-time data flow ensuring <60 second latency from API poll to Gold table, graceful handling of late data, and reliable restart behavior.

Output: Job configurations, notebook wrapper, streaming tests, and cached fallback data for demo reliability.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-data-foundation/01-RESEARCH.md
@.planning/phases/01-data-foundation/01-01-SUMMARY.md
@.planning/phases/01-data-foundation/01-02-SUMMARY.md
</context>

<interfaces>
<!-- Key exports from Plan 01 -->
From src/ingestion/poll_job.py:
```python
def poll_and_write(landing_path: str, bbox: dict) -> int:
    """Poll OpenSky API and write to landing zone. Returns state count."""
```

From src/config/settings.py:
```python
LANDING_PATH: str = "/mnt/data/landing"
SFO_BBOX: dict = {"lamin": 36.0, "lamax": 39.0, "lomin": -124.0, "lomax": -121.0}
POLL_INTERVAL_SECONDS: int = 30
```

From src/ingestion/circuit_breaker.py:
```python
api_circuit_breaker: APICircuitBreaker  # singleton instance
```

<!-- Key exports from Plan 02 -->
From databricks/dlt_pipeline_config.json:
```json
{
  "name": "airport_digital_twin_pipeline",
  "target": "airport_digital_twin",
  "configuration": {
    "pipelines.trigger.interval": "30 seconds"
  }
}
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Databricks job wrapper and notebook</name>
  <files>src/ingestion/databricks_job.py, databricks/notebooks/run_poll.py, databricks/jobs/poll_job_config.json</files>
  <behavior>
    - Test: run_poll_job() calls poll_and_write with correct parameters
    - Test: Job config JSON is valid and references correct notebook path
    - Test: Notebook imports and invokes run_poll_job
  </behavior>
  <action>
    Create src/ingestion/databricks_job.py:
    - Import poll_and_write from poll_job
    - Import settings (LANDING_PATH, SFO_BBOX, POLL_INTERVAL_SECONDS)
    - run_poll_job() function that:
      1. Logs start time
      2. Calls poll_and_write(LANDING_PATH, SFO_BBOX)
      3. Logs result count and duration
      4. Returns dict with timestamp, count, duration, status

    Create databricks/jobs/ directory.

    Create databricks/jobs/poll_job_config.json:
    - name: "airport_poll_opensky"
    - tasks array with single task:
      - task_key: "poll_flights"
      - notebook_task:
        - notebook_path: "/Repos/airport-digital-twin/databricks/notebooks/run_poll"
        - source: "WORKSPACE"
      - job_cluster_key: "poll_cluster"
    - job_clusters with single cluster:
      - job_cluster_key: "poll_cluster"
      - new_cluster:
        - spark_version: "14.3.x-scala2.12"
        - node_type_id: "i3.xlarge" (or appropriate for region)
        - num_workers: 0 (single node)
        - spark_conf: spark.databricks.cluster.profile = singleNode
    - schedule:
      - quartz_cron_expression: "0 0/1 * * * ?" (every minute)
      - timezone_id: "UTC"
    - max_concurrent_runs: 1

    Create databricks/notebooks/run_poll.py:
    - Databricks notebook header: # Databricks notebook source
    - Import sys, add src to path if needed
    - from ingestion.databricks_job import run_poll_job
    - result = run_poll_job()
    - dbutils.notebook.exit(json.dumps(result))

    Reference 01-RESEARCH.md for cluster configuration patterns.
  </action>
  <verify>
    <automated>cd /Users/laurent.prat/Documents/lpdev/databricks_airport_digital_twin && python -c "import json; c=json.load(open('databricks/jobs/poll_job_config.json')); assert 'tasks' in c; assert 'schedule' in c; print('Job config valid')"</automated>
  </verify>
  <done>Job config is valid JSON with schedule and notebook task, wrapper module exists</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Streaming tests and fallback data</name>
  <files>tests/test_streaming.py, data/fallback/sample_flights.json</files>
  <behavior>
    - Test: Watermark is set to 2 minutes on Silver table
    - Test: Deduplication uses icao24 + position_time
    - Test: Fallback JSON matches OpenSky API response schema
    - Test: At least 50 sample flights in fallback data
  </behavior>
  <action>
    Create tests/test_streaming.py:
    - test_silver_watermark_configured: Parse silver.py, verify withWatermark call present with "2 minutes"
    - test_silver_deduplication_keys: Parse silver.py, verify dropDuplicates includes icao24 and position_time
    - test_late_data_not_dropped_silently: Verify expect decorator logs (not drops) late data
    - test_checkpoint_path_configurable: Verify checkpoint location not hardcoded (uses config)
    - test_fallback_json_valid_schema: Load sample_flights.json, validate against OpenSkyResponse schema
    - test_fallback_has_minimum_flights: Assert len(data["states"]) >= 50

    Create data/ directory and data/fallback/ subdirectory.

    Create data/fallback/sample_flights.json:
    - Use generate_synthetic_flights(count=100) to create data
    - Write to file with proper formatting
    - Include realistic distribution:
      - ~10% on_ground
      - Various flight phases
      - Positions within SFO_BBOX
    - Add comment field explaining this is for offline demo mode
    - Include timestamp field indicating generation date

    The fallback file serves as:
    1. Cached data when OpenSky API unavailable
    2. Test fixture for integration tests
    3. Offline demo capability
  </action>
  <verify>
    <automated>cd /Users/laurent.prat/Documents/lpdev/databricks_airport_digital_twin && pytest tests/test_streaming.py -v</automated>
  </verify>
  <done>Streaming tests pass, fallback JSON exists with 100+ flights</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Complete data foundation for Airport Digital Twin:
    1. Ingestion layer: OpenSky API client, circuit breaker, synthetic fallback
    2. DLT pipeline: Bronze/Silver/Gold medallion architecture
    3. Unity Catalog: Schema definitions and setup SQL
    4. Streaming: Job configuration, latency handling, checkpoint resilience
    5. Fallback data: 100+ synthetic flights for offline demo
  </what-built>
  <how-to-verify>
    **Local verification (no Databricks required):**
    1. Run full test suite:
       ```bash
       cd /Users/laurent.prat/Documents/lpdev/databricks_airport_digital_twin
       pytest tests/ -v
       ```
       Expected: All tests pass

    2. Check file structure:
       ```bash
       ls -la src/ingestion/
       ls -la src/pipelines/
       ls -la databricks/
       ls -la data/fallback/
       ```
       Expected: All planned files exist

    3. Validate fallback data:
       ```bash
       python -c "import json; d=json.load(open('data/fallback/sample_flights.json')); print(f'{len(d.get(\"states\", []))} flights')"
       ```
       Expected: "100 flights" (or more)

    **Databricks deployment verification (if workspace available):**
    4. Run Unity Catalog setup SQL in Databricks SQL Editor
    5. Import notebooks to Databricks Repos
    6. Create and start DLT pipeline from config
    7. Run poll job once manually
    8. Verify data flows Bronze -> Silver -> Gold in Unity Catalog

    **Success indicators:**
    - [ ] pytest tests/ -v shows all green
    - [ ] All source files exist per plan files_modified
    - [ ] Fallback JSON has 100+ valid flight records
    - [ ] (Optional) DLT pipeline runs successfully in Databricks
  </how-to-verify>
  <resume-signal>Type "approved" if tests pass and structure looks correct, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
Streaming infrastructure complete:
1. Poll job configured to run on schedule
2. Notebook wrapper invokes poll_and_write correctly
3. Watermark and deduplication configured for late data
4. Fallback data available for offline/demo mode
5. All streaming tests pass
</verification>

<success_criteria>
- databricks/jobs/poll_job_config.json has valid schedule configuration
- tests/test_streaming.py passes with watermark and dedup verification
- data/fallback/sample_flights.json has 100+ valid flight records
- Human verification confirms test suite passes and structure is correct
</success_criteria>

<output>
After completion, create `.planning/phases/01-data-foundation/01-03-SUMMARY.md`
</output>
