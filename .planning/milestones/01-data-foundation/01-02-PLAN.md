---
phase: 01-data-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pipelines/__init__.py
  - src/pipelines/bronze.py
  - src/pipelines/silver.py
  - src/pipelines/gold.py
  - src/schemas/flight.py
  - databricks/setup_unity_catalog.sql
  - databricks/dlt_pipeline_config.json
  - tests/test_dlt.py
  - tests/test_unity_catalog.py
autonomous: true
requirements:
  - DATA-03
  - DATA-04
  - DATA-05

must_haves:
  truths:
    - "DLT Bronze table ingests raw JSON from landing zone"
    - "DLT Silver table applies data quality expectations and deduplication"
    - "DLT Gold table aggregates to current flight status"
    - "All tables registered in Unity Catalog under airport_digital_twin catalog"
    - "Lineage flows from Bronze to Silver to Gold"
  artifacts:
    - path: "src/pipelines/bronze.py"
      provides: "DLT Bronze table definition with Auto Loader"
      contains: "@dlt.table"
    - path: "src/pipelines/silver.py"
      provides: "DLT Silver table with data quality expectations"
      contains: "@dlt.expect"
    - path: "src/pipelines/gold.py"
      provides: "DLT Gold table with flight status aggregation"
      contains: "flight_status_gold"
    - path: "databricks/setup_unity_catalog.sql"
      provides: "SQL script for catalog and schema setup"
      contains: "CREATE CATALOG"
    - path: "databricks/dlt_pipeline_config.json"
      provides: "DLT pipeline configuration for deployment"
  key_links:
    - from: "src/pipelines/silver.py"
      to: "src/pipelines/bronze.py"
      via: "dlt.read_stream"
      pattern: 'dlt\\.read_stream\\("flights_bronze"\\)'
    - from: "src/pipelines/gold.py"
      to: "src/pipelines/silver.py"
      via: "dlt.read_stream"
      pattern: 'dlt\\.read_stream\\("flights_silver"\\)'
    - from: "databricks/dlt_pipeline_config.json"
      to: "src/pipelines/"
      via: "notebook library paths"
      pattern: "libraries.*notebook.*path"
---

<objective>
Create the DLT medallion architecture (Bronze/Silver/Gold) and configure Unity Catalog for data governance and lineage tracking.

Purpose: Establish the streaming data transformation pipeline that converts raw API data into business-ready flight status, with full lineage visibility in Unity Catalog for demo purposes.

Output: DLT pipeline definitions (3 Python notebooks), Unity Catalog setup SQL, pipeline configuration JSON.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-data-foundation/01-RESEARCH.md
</context>

<interfaces>
<!-- Types from Plan 01 that this plan depends on -->

From src/schemas/opensky.py (created in Plan 01):
```python
class StateVector(BaseModel):
    icao24: str
    callsign: Optional[str]
    origin_country: str
    # ... other fields

class OpenSkyResponse(BaseModel):
    time: int
    states: Optional[List[List]] = None
```

From src/config/settings.py (created in Plan 01):
```python
LANDING_PATH: str = "/mnt/data/landing"
SFO_BBOX: dict
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Unity Catalog setup and domain schema</name>
  <files>databricks/setup_unity_catalog.sql, src/schemas/flight.py, tests/test_unity_catalog.py</files>
  <action>
    Create databricks/ directory for Databricks-specific artifacts.

    Create databricks/setup_unity_catalog.sql:
    - CREATE CATALOG IF NOT EXISTS airport_digital_twin
    - USE CATALOG airport_digital_twin
    - CREATE SCHEMA IF NOT EXISTS bronze COMMENT 'Raw ingested data from external sources'
    - CREATE SCHEMA IF NOT EXISTS silver COMMENT 'Cleaned and validated data'
    - CREATE SCHEMA IF NOT EXISTS gold COMMENT 'Business-ready aggregated data'
    - Include GRANT statements (commented out with placeholder for service principal)
    - Add header comment explaining this script runs once before DLT deployment

    Create src/schemas/flight.py:
    - FlightPosition dataclass/model for Silver layer schema
      - icao24: str (primary key)
      - callsign: Optional[str]
      - origin_country: str
      - position_time: datetime
      - last_contact: datetime
      - longitude: float
      - latitude: float
      - baro_altitude: Optional[float]
      - on_ground: bool
      - velocity: Optional[float]
      - true_track: Optional[float]
      - vertical_rate: Optional[float]
      - geo_altitude: Optional[float]
      - squawk: Optional[str]
      - position_source: int
      - category: Optional[int]

    - FlightStatus dataclass/model for Gold layer schema
      - icao24: str (primary key)
      - callsign: Optional[str]
      - origin_country: str
      - last_seen: datetime
      - longitude: float
      - latitude: float
      - altitude: Optional[float]
      - velocity: Optional[float]
      - heading: Optional[float]
      - on_ground: bool
      - vertical_rate: Optional[float]
      - flight_phase: str (ground/climbing/descending/cruising)
      - data_source: str (opensky/synthetic/fallback)

    Create tests/test_unity_catalog.py with test stubs:
    - test_catalog_sql_syntax: Validates SQL file is parseable
    - test_flight_position_schema: Validates FlightPosition has required fields
    - test_flight_status_schema: Validates FlightStatus has flight_phase enum values
  </action>
  <verify>
    <automated>cd /Users/laurent.prat/Documents/lpdev/databricks_airport_digital_twin && pytest tests/test_unity_catalog.py -v</automated>
  </verify>
  <done>SQL file exists, domain schemas defined, tests pass</done>
</task>

<task type="auto">
  <name>Task 2: DLT Bronze and Silver pipeline definitions</name>
  <files>src/pipelines/__init__.py, src/pipelines/bronze.py, src/pipelines/silver.py, tests/test_dlt.py</files>
  <action>
    Create src/pipelines/__init__.py (empty).

    Create src/pipelines/bronze.py:
    - Import dlt, pyspark.sql.functions (current_timestamp, input_file_name)
    - @dlt.table decorator with:
      - name="flights_bronze"
      - comment="Raw flight data from OpenSky API"
      - table_properties={"quality": "bronze", "pipelines.autoOptimize.managed": "true"}
    - Function flights_bronze():
      - spark.readStream.format("cloudFiles")
      - option cloudFiles.format = "json"
      - option cloudFiles.schemaLocation = configurable path
      - option cloudFiles.inferColumnTypes = "true"
      - load from landing zone path (parameterized)
      - Add _ingested_at column with current_timestamp()
      - Add _source_file column with input_file_name()

    Create src/pipelines/silver.py:
    - Import dlt, pyspark.sql.functions (col, explode, from_unixtime, to_timestamp)
    - @dlt.table decorator with name="flights_silver", comment="Cleaned and normalized flight positions"
    - @dlt.expect_or_drop("valid_position", "latitude IS NOT NULL AND longitude IS NOT NULL")
    - @dlt.expect_or_drop("valid_icao24", "icao24 IS NOT NULL AND LENGTH(icao24) = 6")
    - @dlt.expect("valid_altitude", "baro_altitude >= 0 OR baro_altitude IS NULL")
    - Function flights_silver():
      - dlt.read_stream("flights_bronze")
      - explode states array into individual rows
      - Extract all 17 fields from state array by index position
      - Cast types appropriately (double, boolean, int)
      - Apply withWatermark("position_time", "2 minutes")
      - Apply dropDuplicates(["icao24", "position_time"])

    Reference patterns from 01-RESEARCH.md Bronze and Silver sections.

    Create tests/test_dlt.py with stubs:
    - test_bronze_table_has_metadata_columns: Check _ingested_at and _source_file
    - test_silver_has_data_quality_expectations: Check expect decorators exist
    - test_silver_deduplicates_by_icao_time: Verify watermark and dropDuplicates
  </action>
  <verify>
    <automated>cd /Users/laurent.prat/Documents/lpdev/databricks_airport_digital_twin && pytest tests/test_dlt.py -v</automated>
  </verify>
  <done>Bronze and Silver DLT definitions complete, tests validate structure</done>
</task>

<task type="auto">
  <name>Task 3: DLT Gold pipeline and configuration</name>
  <files>src/pipelines/gold.py, databricks/dlt_pipeline_config.json</files>
  <action>
    Create src/pipelines/gold.py:
    - Import dlt, pyspark.sql.functions (col, max, first, when, lit)
    - @dlt.table decorator with name="flight_status_gold", comment="Current status of all tracked flights"
    - Function flight_status_gold():
      - dlt.read_stream("flights_silver")
      - groupBy("icao24") for per-aircraft aggregation
      - Aggregations:
        - max("position_time").alias("last_seen")
        - first("callsign", ignorenulls=True).alias("callsign")
        - first for: origin_country, longitude, latitude, baro_altitude as altitude, velocity, true_track as heading, on_ground, vertical_rate, data_source
      - Add flight_phase computed column:
        - when on_ground: "ground"
        - when vertical_rate > 5: "climbing"
        - when vertical_rate < -5: "descending"
        - otherwise: "cruising"

    Create databricks/dlt_pipeline_config.json:
    - name: "airport_digital_twin_pipeline"
    - storage: "/mnt/data/dlt/airport" (configurable)
    - target: "airport_digital_twin" (Unity Catalog)
    - continuous: false (start with triggered for development)
    - development: true (for initial testing)
    - clusters: autoscale 1-4 workers, ENHANCED mode
    - libraries: array with notebook paths for bronze, silver, gold
    - configuration: pipelines.trigger.interval = "30 seconds"

    Add comments in JSON (as separate README or inline where possible) explaining:
    - Change continuous: true for production/demo
    - Change development: false when ready for production
    - Update notebook paths to match Repos deployment path

    Update tests/test_dlt.py:
    - test_gold_computes_flight_phase: Verify flight_phase logic
    - test_gold_aggregates_by_icao24: Verify groupBy key
  </action>
  <verify>
    <automated>cd /Users/laurent.prat/Documents/lpdev/databricks_airport_digital_twin && pytest tests/test_dlt.py -v && python -c "import json; json.load(open('databricks/dlt_pipeline_config.json'))"</automated>
  </verify>
  <done>Gold DLT definition complete, pipeline config is valid JSON, all DLT tests pass</done>
</task>

</tasks>

<verification>
DLT medallion architecture complete:
1. Unity Catalog SQL ready for deployment
2. Bronze table ingests from landing zone with metadata
3. Silver table applies data quality and deduplication
4. Gold table provides current flight status with flight_phase
5. Pipeline configuration ready for DLT deployment
</verification>

<success_criteria>
- databricks/setup_unity_catalog.sql exists with CREATE CATALOG/SCHEMA statements
- src/pipelines/ has bronze.py, silver.py, gold.py with @dlt.table decorators
- databricks/dlt_pipeline_config.json is valid JSON
- All tests in test_dlt.py and test_unity_catalog.py pass
</success_criteria>

<output>
After completion, create `.planning/phases/01-data-foundation/01-02-SUMMARY.md`
</output>
