---
phase: 01-data-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/ingestion/__init__.py
  - src/ingestion/opensky_client.py
  - src/ingestion/fallback.py
  - src/ingestion/circuit_breaker.py
  - src/ingestion/poll_job.py
  - src/schemas/__init__.py
  - src/schemas/opensky.py
  - src/config/__init__.py
  - src/config/settings.py
  - tests/conftest.py
  - tests/test_ingestion.py
  - pyproject.toml
autonomous: true
requirements:
  - DATA-01
  - DATA-02

must_haves:
  truths:
    - "API client fetches flight data from OpenSky Network"
    - "Circuit breaker activates after repeated API failures"
    - "Synthetic data generator produces valid flight positions"
    - "Poll job writes JSON files to landing zone"
  artifacts:
    - path: "src/ingestion/opensky_client.py"
      provides: "OpenSky API client with OAuth2 auth and retry logic"
      exports: ["OpenSkyClient", "OpenSkyResponse", "StateVector"]
    - path: "src/ingestion/fallback.py"
      provides: "Synthetic flight data generator"
      exports: ["generate_synthetic_flights"]
    - path: "src/ingestion/circuit_breaker.py"
      provides: "Circuit breaker for API failover"
      exports: ["APICircuitBreaker", "api_circuit_breaker"]
    - path: "src/ingestion/poll_job.py"
      provides: "Databricks job entrypoint for polling"
      exports: ["poll_and_write"]
    - path: "tests/test_ingestion.py"
      provides: "Test coverage for ingestion components"
  key_links:
    - from: "src/ingestion/poll_job.py"
      to: "src/ingestion/opensky_client.py"
      via: "import and call get_states()"
      pattern: "OpenSkyClient.*get_states"
    - from: "src/ingestion/poll_job.py"
      to: "src/ingestion/circuit_breaker.py"
      via: "check circuit state before API call"
      pattern: "api_circuit_breaker\\.can_execute"
    - from: "src/ingestion/poll_job.py"
      to: "src/ingestion/fallback.py"
      via: "call fallback when circuit open"
      pattern: "generate_synthetic_flights"
---

<objective>
Build the data ingestion layer that polls OpenSky Network API for real-time flight data and writes to a landing zone for downstream DLT consumption.

Purpose: Establish reliable data source with automatic failover to synthetic data when API is unavailable, ensuring the demo always has flight data to display.

Output: Working Python modules for API client, circuit breaker, synthetic data generation, and polling job. Test scaffolding for the phase.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-data-foundation/01-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Project setup and test scaffolding (Wave 0)</name>
  <files>pyproject.toml, tests/conftest.py, tests/test_ingestion.py, src/__init__.py, src/ingestion/__init__.py, src/schemas/__init__.py, src/config/__init__.py</files>
  <behavior>
    - Test: OpenSkyClient.get_states() returns OpenSkyResponse with states list
    - Test: generate_synthetic_flights(count=10) returns dict with 10 states
    - Test: APICircuitBreaker opens after 5 failures, closes after recovery_timeout
    - Test: poll_and_write() creates JSON file in landing path
  </behavior>
  <action>
    Create pyproject.toml with project metadata and dependencies:
    - name: airport-digital-twin
    - python: >=3.10
    - dependencies: requests>=2.31, pydantic>=2.5, tenacity>=8.2, circuitbreaker>=1.4, Faker>=22.0
    - dev-dependencies: pytest>=8.0, pytest-mock>=3.12

    Create package structure:
    - src/__init__.py (empty)
    - src/ingestion/__init__.py (empty)
    - src/schemas/__init__.py (empty)
    - src/config/__init__.py (empty)

    Create tests/conftest.py with fixtures:
    - mock_opensky_response: Returns sample OpenSkyResponse dict
    - mock_landing_path: Creates temp directory for landing zone tests

    Create tests/test_ingestion.py with test stubs (initially failing):
    - test_opensky_client_returns_states: Tests API client returns valid response
    - test_fallback_generates_valid_flights: Tests synthetic data format
    - test_circuit_breaker_opens_after_failures: Tests circuit breaker state
    - test_poll_job_writes_to_landing: Tests file creation

    Use requests-mock or unittest.mock to avoid real API calls in tests.
  </action>
  <verify>
    <automated>cd /Users/laurent.prat/Documents/lpdev/databricks_airport_digital_twin && pip install -e ".[dev]" && pytest tests/test_ingestion.py -v --collect-only</automated>
  </verify>
  <done>pyproject.toml exists with all dependencies, test file has 4+ test functions collected (will fail - RED phase)</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: OpenSky API client with OAuth2 and retry</name>
  <files>src/ingestion/opensky_client.py, src/schemas/opensky.py, src/config/settings.py</files>
  <behavior>
    - Test: get_states() with valid bbox returns OpenSkyResponse
    - Test: get_states() retries 3 times on transient failure
    - Test: OAuth2 token fetched when credentials configured
    - Test: StateVector model validates ICAO24 is 6 characters
  </behavior>
  <action>
    Create src/config/settings.py:
    - Define Settings class using pydantic-settings (or os.getenv)
    - OPENSKY_CLIENT_ID: Optional[str] = None
    - OPENSKY_CLIENT_SECRET: Optional[str] = None
    - LANDING_PATH: str = "/mnt/data/landing"
    - POLL_INTERVAL_SECONDS: int = 30
    - SFO_BBOX: dict with lamin=36.0, lamax=39.0, lomin=-124.0, lomax=-121.0

    Create src/schemas/opensky.py:
    - StateVector pydantic model (17 fields from OpenSky API response)
    - OpenSkyResponse model with time: int and states: Optional[List[List]]

    Create src/ingestion/opensky_client.py:
    - OpenSkyClient class with BASE_URL and AUTH_URL class variables
    - __init__ loads credentials from settings
    - _get_token() method for OAuth2 client credentials flow
    - get_states(bbox, icao24) method with @retry decorator (3 attempts, exponential backoff)
    - Parse response into OpenSkyResponse model
    - Handle rate limit (429) by raising specific exception

    Reference patterns from 01-RESEARCH.md "OpenSky API Client with Retry and Auth" section.
  </action>
  <verify>
    <automated>cd /Users/laurent.prat/Documents/lpdev/databricks_airport_digital_twin && pytest tests/test_ingestion.py -k "opensky" -v</automated>
  </verify>
  <done>test_opensky_client_returns_states passes with mocked response, retry decorator configured</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Circuit breaker and synthetic data fallback</name>
  <files>src/ingestion/circuit_breaker.py, src/ingestion/fallback.py, src/ingestion/poll_job.py</files>
  <behavior>
    - Test: Circuit breaker opens after 5 consecutive failures
    - Test: Circuit breaker transitions to half-open after recovery_timeout
    - Test: Synthetic flights have valid ICAO24 (6 hex chars)
    - Test: Synthetic flights have positions within bbox
    - Test: poll_and_write creates JSON with timestamp and states
  </behavior>
  <action>
    Create src/ingestion/circuit_breaker.py:
    - APICircuitBreaker class with failure_threshold=5, recovery_timeout=60
    - States: closed (normal), open (failing), half-open (testing)
    - record_failure() increments counter, transitions to open
    - record_success() resets counter, transitions to closed
    - can_execute() returns True if closed or (open and timeout elapsed)
    - api_circuit_breaker module-level singleton instance

    Create src/ingestion/fallback.py:
    - CALLSIGN_PREFIXES list (UAL, DAL, AAL, SWA, JBU, ASA, FFT, SKW)
    - generate_synthetic_flights(count, bbox) function
    - Generate ICAO24 as 6 hex characters (use Faker.hexify)
    - Generate positions within bbox bounds
    - Return dict matching OpenSky API structure: {"time": int, "states": [[...]]}

    Create src/ingestion/poll_job.py:
    - Import OpenSkyClient, api_circuit_breaker, generate_synthetic_flights
    - poll_and_write(landing_path, bbox) function:
      1. Check circuit breaker state
      2. If open: use synthetic data, source="synthetic"
      3. If closed/half-open: try API call
      4. On API success: record_success(), source="opensky"
      5. On API failure: record_failure(), use synthetic, source="fallback"
      6. Write JSON to {landing_path}/{timestamp}.json
      7. Return count of states written

    Reference patterns from 01-RESEARCH.md "Circuit Breaker" and "Synthetic Data Generator" sections.
  </action>
  <verify>
    <automated>cd /Users/laurent.prat/Documents/lpdev/databricks_airport_digital_twin && pytest tests/test_ingestion.py -v</automated>
  </verify>
  <done>All tests in test_ingestion.py pass - circuit breaker, fallback, and poll_job working</done>
</task>

</tasks>

<verification>
All ingestion components working:
1. OpenSky client can fetch flight data (mocked in tests)
2. Circuit breaker properly transitions between states
3. Synthetic data matches API response format
4. Poll job writes valid JSON to landing zone
</verification>

<success_criteria>
- pytest tests/test_ingestion.py passes with 4+ tests green
- src/ingestion/ has all 4 Python modules with implementations
- poll_and_write() creates JSON file when called
- Circuit breaker opens after 5 failures and recovers
</success_criteria>

<output>
After completion, create `.planning/phases/01-data-foundation/01-01-SUMMARY.md`
</output>
