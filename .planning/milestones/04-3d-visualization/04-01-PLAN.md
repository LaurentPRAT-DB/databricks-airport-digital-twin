---
phase: 04-3d-visualization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/frontend/package.json
  - app/frontend/src/components/Map3D/Map3D.tsx
  - app/frontend/src/components/Map3D/AirportScene.tsx
  - app/frontend/src/components/Map3D/index.ts
  - app/frontend/src/constants/airport3D.ts
autonomous: true
requirements:
  - VIZ3D-01

must_haves:
  truths:
    - "Three.js renders 3D scene in React component"
    - "Airport scene includes terminal building geometry"
    - "Runways and taxiways rendered as 3D planes"
    - "Scene has proper lighting and camera setup"
  artifacts:
    - path: "app/frontend/src/components/Map3D/Map3D.tsx"
      provides: "Main 3D map component with Three.js canvas"
      exports: ["Map3D"]
    - path: "app/frontend/src/components/Map3D/AirportScene.tsx"
      provides: "Airport 3D geometry (terminal, runways, taxiways)"
      exports: ["AirportScene"]
    - path: "app/frontend/src/constants/airport3D.ts"
      provides: "3D coordinates and dimensions for airport elements"
      exports: ["AIRPORT_3D_CONFIG"]
---

<objective>
Set up Three.js in React and create the 3D airport scene with terminal, runways, and taxiways.

Purpose: Establish the 3D visualization foundation that will display live flight positions.

Output: Working 3D scene showing a stylized airport environment.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@app/frontend/src/constants/airportLayout.ts
@app/frontend/package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install Three.js dependencies</name>
  <files>app/frontend/package.json</files>
  <action>
    Install Three.js and React Three Fiber (R3F) for React integration:

    cd app/frontend && npm install three @react-three/fiber @react-three/drei

    @react-three/fiber: React renderer for Three.js
    @react-three/drei: Useful helpers (OrbitControls, etc.)

    Also install types:
    npm install -D @types/three
  </action>
  <verify>
    <automated>cd /Users/laurent.prat/Documents/lpdev/databricks_airport_digital_twin/app/frontend && npm ls three @react-three/fiber @react-three/drei</automated>
  </verify>
  <done>Three.js packages installed</done>
</task>

<task type="auto">
  <name>Task 2: Create 3D constants and configuration</name>
  <files>app/frontend/src/constants/airport3D.ts</files>
  <action>
    Create app/frontend/src/constants/airport3D.ts:

    Export AIRPORT_3D_CONFIG object:
    - center: { x: 0, y: 0, z: 0 } (scene center)
    - scale: 0.001 (convert meters to scene units)

    - terminal: {
        position: { x: 0, y: 0, z: 0 },
        dimensions: { width: 200, height: 20, depth: 80 },
        color: 0x4a90d9 (blue-gray)
      }

    - runways: [
        {
          id: "28L",
          start: { x: -500, y: 0, z: -100 },
          end: { x: 500, y: 0, z: -100 },
          width: 45,
          color: 0x333333 (dark gray)
        },
        {
          id: "28R",
          start: { x: -500, y: 0, z: 100 },
          end: { x: 500, y: 0, z: 100 },
          width: 45,
          color: 0x333333
        }
      ]

    - taxiways: [
        {
          id: "A",
          points: [{ x: 0, y: 0, z: -100 }, { x: 0, y: 0, z: -50 }, { x: 0, y: 0, z: 0 }],
          width: 20,
          color: 0x555555
        },
        {
          id: "B",
          points: [{ x: 0, y: 0, z: 100 }, { x: 0, y: 0, z: 50 }, { x: 0, y: 0, z: 0 }],
          width: 20,
          color: 0x555555
        }
      ]

    - ground: {
        size: 2000,
        color: 0x228b22 (grass green)
      }

    - lighting: {
        ambient: { intensity: 0.6 },
        directional: { position: { x: 100, y: 100, z: 50 }, intensity: 0.8 }
      }
  </action>
  <verify>
    <automated>cd /Users/laurent.prat/Documents/lpdev/databricks_airport_digital_twin/app/frontend && npx tsc --noEmit src/constants/airport3D.ts 2>&1 || echo "Check syntax"</automated>
  </verify>
  <done>3D configuration file created</done>
</task>

<task type="auto">
  <name>Task 3: Create AirportScene component</name>
  <files>app/frontend/src/components/Map3D/AirportScene.tsx, app/frontend/src/components/Map3D/index.ts</files>
  <action>
    Create app/frontend/src/components/Map3D/AirportScene.tsx:

    Import from @react-three/fiber and drei.
    Import AIRPORT_3D_CONFIG from constants.

    Component renders:
    1. Ground plane (large flat box with grass color)
    2. Terminal building (box geometry with blue-gray material)
    3. Runways (box geometry, dark gray, positioned correctly)
    4. Taxiways (for each taxiway, create tube or flat plane connecting points)
    5. Runway markings (white lines on runway surfaces)

    Use React Three Fiber JSX syntax:
    - <mesh> for objects
    - <boxGeometry> for shapes
    - <meshStandardMaterial> for materials

    Create app/frontend/src/components/Map3D/index.ts:
    - Export { Map3D } from './Map3D'
    - Export { AirportScene } from './AirportScene'
  </action>
  <verify>
    <automated>cd /Users/laurent.prat/Documents/lpdev/databricks_airport_digital_twin/app/frontend && npx tsc --noEmit 2>&1 | head -10 || echo "Check compilation"</automated>
  </verify>
  <done>AirportScene component created</done>
</task>

<task type="auto">
  <name>Task 4: Create Map3D container component</name>
  <files>app/frontend/src/components/Map3D/Map3D.tsx</files>
  <action>
    Create app/frontend/src/components/Map3D/Map3D.tsx:

    Import { Canvas } from '@react-three/fiber'
    Import { OrbitControls, PerspectiveCamera } from '@react-three/drei'
    Import { AirportScene } from './AirportScene'
    Import AIRPORT_3D_CONFIG from constants.

    Map3D component:
    - Props: className?: string
    - Renders Canvas with:
      - PerspectiveCamera at position [0, 300, 500] looking at origin
      - ambientLight with configured intensity
      - directionalLight with configured position/intensity
      - AirportScene component
      - OrbitControls for camera movement
        - enablePan: true
        - enableZoom: true
        - enableRotate: true
        - maxPolarAngle: Math.PI / 2.1 (prevent going below ground)

    Style the canvas container to fill parent (width: 100%, height: 100%)
  </action>
  <verify>
    <automated>cd /Users/laurent.prat/Documents/lpdev/databricks_airport_digital_twin/app/frontend && npx tsc --noEmit 2>&1 | head -10 || echo "Check compilation"</automated>
  </verify>
  <done>Map3D component created with camera controls</done>
</task>

</tasks>

<verification>
3D scene setup complete:
1. Three.js + React Three Fiber installed
2. Airport 3D configuration defined
3. AirportScene renders terminal, runways, taxiways
4. Map3D provides canvas with camera controls
</verification>

<success_criteria>
- npm packages three, @react-three/fiber, @react-three/drei installed
- Map3D component renders without errors
- AirportScene shows terminal building and runways
- OrbitControls allow pan, zoom, rotate
- TypeScript compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/04-3d-visualization/04-01-SUMMARY.md`
</output>
