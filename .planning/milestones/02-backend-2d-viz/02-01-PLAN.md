---
phase: 02-backend-2d-viz
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app.yaml
  - requirements.txt
  - app/backend/__init__.py
  - app/backend/main.py
  - app/backend/api/__init__.py
  - app/backend/api/routes.py
  - app/backend/api/websocket.py
  - app/backend/services/__init__.py
  - app/backend/services/flight_service.py
  - app/backend/models/__init__.py
  - app/backend/models/flight.py
  - tests/test_backend.py
autonomous: true
requirements:
  - VIZ2D-02

must_haves:
  truths:
    - "FastAPI app starts and serves /health endpoint"
    - "REST endpoint returns flight data from service"
    - "WebSocket endpoint accepts connections and broadcasts data"
    - "Flight service can query Gold table (mocked in tests)"
  artifacts:
    - path: "app/backend/main.py"
      provides: "FastAPI application with CORS and WebSocket"
      exports: ["app"]
    - path: "app/backend/api/routes.py"
      provides: "REST API endpoints for flights"
      exports: ["router"]
    - path: "app/backend/api/websocket.py"
      provides: "WebSocket broadcaster for real-time updates"
      exports: ["FlightBroadcaster", "broadcaster"]
    - path: "app/backend/services/flight_service.py"
      provides: "Flight data retrieval from Gold table"
      exports: ["FlightService", "get_flight_service"]
---

<objective>
Create the FastAPI backend with REST and WebSocket endpoints for serving flight data to the frontend.

Purpose: Establish the API layer that bridges Phase 1's Gold table data to Phase 2's visualization components.

Output: Working FastAPI application with health check, flight list endpoint, and WebSocket broadcasting.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-backend-2d-viz/02-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: APX app structure and FastAPI setup</name>
  <files>app.yaml, requirements.txt, app/backend/__init__.py, app/backend/main.py</files>
  <action>
    Create app/ directory structure for APX framework.

    Create app.yaml (Databricks App config):
    - name: airport-digital-twin
    - entrypoint: uvicorn app.backend.main:app --host 0.0.0.0 --port 8000
    - python_version: "3.10"

    Create requirements.txt:
    - fastapi>=0.109.0
    - uvicorn[standard]>=0.27.0
    - websockets>=12.0
    - pydantic>=2.5.0
    - python-dotenv>=1.0.0

    Create app/backend/__init__.py (empty).

    Create app/backend/main.py:
    - Import FastAPI, CORSMiddleware
    - Create app = FastAPI(title="Airport Digital Twin API")
    - Add CORS middleware allowing all origins (for development)
    - Include router from api.routes
    - Add /health endpoint returning {"status": "healthy"}
    - Add startup event to initialize services
  </action>
  <verify>
    <automated>cd /Users/laurent.prat/Documents/lpdev/databricks_airport_digital_twin && python3 -c "from app.backend.main import app; print('FastAPI app created')"</automated>
  </verify>
  <done>FastAPI app imports without error</done>
</task>

<task type="auto">
  <name>Task 2: Flight models and service layer</name>
  <files>app/backend/models/__init__.py, app/backend/models/flight.py, app/backend/services/__init__.py, app/backend/services/flight_service.py</files>
  <action>
    Create app/backend/models/__init__.py (empty).

    Create app/backend/models/flight.py:
    - FlightPosition(BaseModel): icao24, callsign, latitude, longitude, altitude, velocity, heading, on_ground, vertical_rate, last_seen, data_source, flight_phase
    - FlightListResponse(BaseModel): flights: List[FlightPosition], count: int, timestamp: datetime

    Create app/backend/services/__init__.py (empty).

    Create app/backend/services/flight_service.py:
    - FlightService class with async methods
    - __init__ takes optional connection config
    - async get_flights() -> List[FlightPosition]: queries Gold table
    - For local development: return mock data if no DB connection
    - Use environment variable DATABRICKS_SQL_WAREHOUSE or fallback to mock
    - get_flight_service() dependency injection function
  </action>
  <verify>
    <automated>cd /Users/laurent.prat/Documents/lpdev/databricks_airport_digital_twin && python3 -c "from app.backend.models.flight import FlightPosition; print('Models OK')"</automated>
  </verify>
  <done>Flight models and service import without error</done>
</task>

<task type="auto">
  <name>Task 3: REST and WebSocket endpoints</name>
  <files>app/backend/api/__init__.py, app/backend/api/routes.py, app/backend/api/websocket.py, tests/test_backend.py</files>
  <action>
    Create app/backend/api/__init__.py (empty).

    Create app/backend/api/routes.py:
    - APIRouter with prefix="/api"
    - GET /api/flights: returns FlightListResponse from FlightService
    - GET /api/flights/{icao24}: returns single flight details
    - Dependency injection for FlightService

    Create app/backend/api/websocket.py:
    - FlightBroadcaster class:
      - connections: list of active WebSocket connections
      - async connect(websocket): accept and add to list
      - async disconnect(websocket): remove from list
      - async broadcast(data): send to all connections
    - broadcaster: module-level singleton instance
    - WebSocket endpoint /ws/flights that:
      - Accepts connection
      - Sends initial flight data
      - Keeps connection open for broadcasts

    Create tests/test_backend.py:
    - test_health_endpoint: GET /health returns 200
    - test_flights_endpoint: GET /api/flights returns list
    - test_flight_model_validation: FlightPosition validates correctly
    - test_websocket_connection: WebSocket connects successfully
    - Use FastAPI TestClient and pytest-asyncio
  </action>
  <verify>
    <automated>cd /Users/laurent.prat/Documents/lpdev/databricks_airport_digital_twin && pip install fastapi uvicorn httpx pytest-asyncio -q && pytest tests/test_backend.py -v</automated>
  </verify>
  <done>All backend tests pass</done>
</task>

</tasks>

<verification>
Backend API complete:
1. FastAPI app starts with CORS enabled
2. /health endpoint returns healthy status
3. /api/flights returns flight list
4. WebSocket accepts connections
</verification>

<success_criteria>
- app/backend/main.py creates FastAPI app
- GET /health returns 200 with status
- GET /api/flights returns flight data
- WebSocket /ws/flights accepts connections
- All tests in test_backend.py pass
</success_criteria>

<output>
After completion, create `.planning/phases/02-backend-2d-viz/02-01-SUMMARY.md`
</output>
