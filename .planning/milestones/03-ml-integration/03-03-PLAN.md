---
phase: 03-ml-integration
plan: 03
type: execute
wave: 2
depends_on: [03-01, 03-02]
files_modified:
  - app/backend/api/routes.py
  - app/backend/api/predictions.py
  - app/backend/services/prediction_service.py
  - app/frontend/src/types/flight.ts
  - app/frontend/src/hooks/usePredictions.ts
  - app/frontend/src/components/FlightDetail/FlightDetail.tsx
  - app/frontend/src/components/GateStatus/GateStatus.tsx
  - tests/test_backend.py
autonomous: true
requirements:
  - ML-05
  - UI-03
  - UI-04

must_haves:
  truths:
    - "Prediction API endpoints respond within 2 seconds"
    - "Frontend displays delay predictions with confidence"
    - "Gate recommendations shown in UI"
    - "Congestion areas highlighted on map"
  artifacts:
    - path: "app/backend/api/predictions.py"
      provides: "Prediction API endpoints"
      exports: ["prediction_router"]
    - path: "app/backend/services/prediction_service.py"
      provides: "Prediction service orchestrating all models"
      exports: ["PredictionService"]
    - path: "app/frontend/src/hooks/usePredictions.ts"
      provides: "React hook for fetching predictions"
      exports: ["usePredictions"]
---

<objective>
Integrate ML models with the backend API and frontend UI.

Purpose: Expose predictions via REST API and display them in the frontend visualization.

Output: Working prediction endpoints showing delay, gate, and congestion data in the UI.
</objective>

<context>
@.planning/PROJECT.md
@src/ml/delay_model.py
@src/ml/gate_model.py
@src/ml/congestion_model.py
@app/backend/api/routes.py
@app/frontend/src/components/FlightDetail/FlightDetail.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Prediction service layer</name>
  <files>app/backend/services/prediction_service.py</files>
  <action>
    Create app/backend/services/prediction_service.py:

    - PredictionService class:
      - __init__():
        - Initialize DelayPredictor
        - Initialize GateRecommender
        - Initialize CongestionPredictor

      - async get_flight_predictions(flights: List[dict]) -> dict:
        - Run all predictions in parallel (using asyncio.gather)
        - Return {
            "delays": {icao24: DelayPrediction},
            "gates": {icao24: GateRecommendation},
            "congestion": List[AreaCongestion]
          }

      - async get_delay_prediction(flight: dict) -> DelayPrediction:
        - Get single flight delay prediction

      - async get_gate_recommendations(flight: dict, top_k: int = 3) -> List[GateRecommendation]:
        - Get gate recommendations for single flight

      - async get_congestion() -> List[AreaCongestion]:
        - Get current congestion for all areas

    - get_prediction_service() dependency function
  </action>
  <verify>
    <automated>cd /Users/laurent.prat/Documents/lpdev/databricks_airport_digital_twin && uv run python -c "from app.backend.services.prediction_service import PredictionService; p = PredictionService(); print('Prediction service OK')"</automated>
  </verify>
  <done>Prediction service instantiates without error</done>
</task>

<task type="auto">
  <name>Task 2: Prediction API endpoints</name>
  <files>app/backend/api/predictions.py, app/backend/api/routes.py</files>
  <action>
    Create app/backend/api/predictions.py:

    - prediction_router = APIRouter(prefix="/api/predictions")

    - Pydantic response models:
      - DelayPredictionResponse: delay_minutes, confidence, category
      - GateRecommendationResponse: gate_id, score, reasons, taxi_time
      - CongestionResponse: area_id, area_type, level, flight_count, wait_minutes

    - GET /api/predictions/delays:
      - Query param: icao24 (optional, for single flight)
      - Returns all flight delay predictions or single flight

    - GET /api/predictions/gates/{icao24}:
      - Path param: icao24
      - Query param: top_k (default 3)
      - Returns gate recommendations for flight

    - GET /api/predictions/congestion:
      - Returns all area congestion levels

    - GET /api/predictions/bottlenecks:
      - Returns only HIGH/CRITICAL congestion areas

    Update app/backend/api/routes.py:
    - Import and include prediction_router

    Update app/backend/main.py:
    - Include prediction_router
  </action>
  <verify>
    <automated>cd /Users/laurent.prat/Documents/lpdev/databricks_airport_digital_twin && uv run python -c "from app.backend.api.predictions import prediction_router; print('Prediction routes OK')"</automated>
  </verify>
  <done>Prediction router imports without error</done>
</task>

<task type="auto">
  <name>Task 3: Frontend prediction types and hook</name>
  <files>app/frontend/src/types/flight.ts, app/frontend/src/hooks/usePredictions.ts</files>
  <action>
    Update app/frontend/src/types/flight.ts:
    - Add DelayPrediction interface: delay_minutes, confidence, category
    - Add GateRecommendation interface: gate_id, score, reasons, taxi_time
    - Add CongestionArea interface: area_id, area_type, level, flight_count, wait_minutes
    - Add FlightWithPredictions extending Flight with predictions

    Create app/frontend/src/hooks/usePredictions.ts:
    - usePredictions(flights: Flight[]) hook:
      - Use TanStack Query
      - Fetch from /api/predictions/delays
      - Return { delays, gates, congestion, isLoading, error }

    - useDelayPrediction(icao24: string):
      - Fetch single flight delay

    - useCongestion():
      - Fetch congestion data
      - Refetch every 10 seconds
  </action>
  <verify>
    <automated>cd /Users/laurent.prat/Documents/lpdev/databricks_airport_digital_twin/app/frontend && npx tsc --noEmit 2>&1 | head -20 || echo "Check for errors"</automated>
  </verify>
  <done>TypeScript compiles without errors</done>
</task>

<task type="auto">
  <name>Task 4: Update UI components with predictions</name>
  <files>app/frontend/src/components/FlightDetail/FlightDetail.tsx, app/frontend/src/components/GateStatus/GateStatus.tsx</files>
  <action>
    Update FlightDetail.tsx:
    - Import usePredictions hook
    - Display delay prediction:
      - Show delay_minutes with color coding (green/yellow/orange/red)
      - Show confidence as percentage
      - Show category badge
    - Display gate recommendation if flight is arriving:
      - Show recommended gate with score
      - List reasons

    Update GateStatus.tsx:
    - Import useCongestion hook
    - Add congestion indicator per gate area
    - Color code by congestion level:
      - LOW: green
      - MODERATE: yellow
      - HIGH: orange
      - CRITICAL: red
    - Show estimated wait time
  </action>
  <verify>
    <automated>cd /Users/laurent.prat/Documents/lpdev/databricks_airport_digital_twin/app/frontend && npx tsc --noEmit</automated>
  </verify>
  <done>Components compile without TypeScript errors</done>
</task>

<task type="auto">
  <name>Task 5: Backend prediction tests</name>
  <files>tests/test_backend.py</files>
  <action>
    Append to tests/test_backend.py:

    class TestPredictionEndpoints:
      - test_delays_endpoint: GET /api/predictions/delays returns 200
      - test_delay_response_format: verify response has required fields
      - test_gates_endpoint: GET /api/predictions/gates/{icao24} returns recommendations
      - test_congestion_endpoint: GET /api/predictions/congestion returns areas
      - test_bottlenecks_endpoint: GET /api/predictions/bottlenecks filters correctly
      - test_prediction_performance: verify response under 2 seconds
  </action>
  <verify>
    <automated>cd /Users/laurent.prat/Documents/lpdev/databricks_airport_digital_twin && uv run pytest tests/test_backend.py -v -k "prediction"</automated>
  </verify>
  <done>Prediction endpoint tests pass</done>
</task>

</tasks>

<verification>
ML integration complete:
1. Prediction API endpoints working (/api/predictions/*)
2. Frontend displays delay predictions with confidence
3. Gate recommendations shown for arriving flights
4. Congestion levels displayed in gate status
5. All tests pass
</verification>

<success_criteria>
- GET /api/predictions/delays returns flight delays
- GET /api/predictions/gates/{icao24} returns recommendations
- GET /api/predictions/congestion returns area congestion
- Frontend shows predictions in FlightDetail
- Response time under 2 seconds
- All prediction tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/03-ml-integration/03-03-SUMMARY.md`
</output>
