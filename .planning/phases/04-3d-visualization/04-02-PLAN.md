---
phase: 04-3d-visualization
plan: 02
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - app/frontend/src/components/Map3D/Aircraft3D.tsx
  - app/frontend/src/components/Map3D/AirportScene.tsx
  - app/frontend/src/components/Map3D/Map3D.tsx
  - app/frontend/src/App.tsx
autonomous: true
requirements:
  - VIZ3D-02
  - VIZ3D-03
  - VIZ3D-04

must_haves:
  truths:
    - "Aircraft models positioned at correct 3D coordinates"
    - "Positions update smoothly with flight data changes"
    - "User can switch between 2D and 3D views"
    - "Aircraft heading reflected in model rotation"
  artifacts:
    - path: "app/frontend/src/components/Map3D/Aircraft3D.tsx"
      provides: "3D aircraft model component"
      exports: ["Aircraft3D"]
---

<objective>
Add 3D aircraft models that update with real-time flight data and integrate 3D view into the app.

Purpose: Complete the 3D visualization with live aircraft positions that update as new data arrives.

Output: Fully functional 3D view with aircraft showing current positions and headings.
</objective>

<context>
@.planning/PROJECT.md
@app/frontend/src/components/Map3D/Map3D.tsx
@app/frontend/src/components/Map3D/AirportScene.tsx
@app/frontend/src/types/flight.ts
@app/frontend/src/hooks/useFlights.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Aircraft3D component</name>
  <files>app/frontend/src/components/Map3D/Aircraft3D.tsx</files>
  <action>
    Create app/frontend/src/components/Map3D/Aircraft3D.tsx:

    Import from @react-three/fiber and drei.
    Import Flight type.

    Props interface:
    - flight: Flight
    - selected?: boolean
    - onClick?: () => void

    Convert lat/lon to 3D coordinates:
    - Create function latLonTo3D(lat, lon, centerLat, centerLon, scale):
      - x = (lon - centerLon) * scale * 111000 (approx meters per degree)
      - z = (lat - centerLat) * scale * 111000
      - y = (altitude || 0) * 0.3048 * scale (feet to meters to scene)
      - Return { x, y, z }

    Aircraft3D component:
    - Use simple cone/arrow geometry for aircraft shape
    - Position based on lat/lon conversion
    - Rotate Y axis based on heading (convert to radians)
    - Color: selected ? 0xff6600 : 0xffffff (orange when selected, white otherwise)
    - Add onClick handler to mesh
    - Use <Html> from drei to show callsign label on hover

    Aircraft geometry (simplified plane shape):
    - Body: cylinder or cone pointing forward
    - Wings: flat box on sides
    - Or use a simple arrow/triangle shape for performance
  </action>
  <verify>
    <automated>cd /Users/laurent.prat/Documents/lpdev/databricks_airport_digital_twin/app/frontend && npx tsc --noEmit 2>&1 | head -10 || echo "Check compilation"</automated>
  </verify>
  <done>Aircraft3D component renders aircraft at position</done>
</task>

<task type="auto">
  <name>Task 2: Integrate aircraft into AirportScene</name>
  <files>app/frontend/src/components/Map3D/AirportScene.tsx, app/frontend/src/components/Map3D/Map3D.tsx</files>
  <action>
    Update AirportScene.tsx:
    - Add props: flights: Flight[], selectedFlight: string | null, onSelectFlight: (icao24: string) => void
    - Map flights to Aircraft3D components
    - Pass selected state and onClick handler

    Update Map3D.tsx:
    - Add props: flights: Flight[], selectedFlight: string | null, onSelectFlight: (icao24: string) => void
    - Pass props to AirportScene
    - Add useFrame hook for smooth animation updates (optional)

    Export updated Map3D index.ts with new props interface.
  </action>
  <verify>
    <automated>cd /Users/laurent.prat/Documents/lpdev/databricks_airport_digital_twin/app/frontend && npx tsc --noEmit 2>&1 | head -10 || echo "Check compilation"</automated>
  </verify>
  <done>Aircraft render in 3D scene based on flight data</done>
</task>

<task type="auto">
  <name>Task 3: Add 2D/3D view toggle in App</name>
  <files>app/frontend/src/App.tsx</files>
  <action>
    Update App.tsx:
    - Add state: viewMode: '2d' | '3d' (default '2d')
    - Add toggle button in header or map area
    - Conditionally render Map (2D) or Map3D based on viewMode
    - Both components receive same flights data and selection state
    - Ensure selection syncs between views

    Toggle button styling:
    - Position in top-right of map area or header
    - Icons or text: "2D" / "3D"
    - Active state highlighted
  </action>
  <verify>
    <automated>cd /Users/laurent.prat/Documents/lpdev/databricks_airport_digital_twin/app/frontend && npx tsc --noEmit 2>&1 | head -10 || echo "Check compilation"</automated>
  </verify>
  <done>2D/3D toggle works in App</done>
</task>

<task type="auto">
  <name>Task 4: Add smooth position updates</name>
  <files>app/frontend/src/components/Map3D/Aircraft3D.tsx</files>
  <action>
    Enhance Aircraft3D for smooth updates:
    - Use useRef to track previous position
    - Use useFrame from @react-three/fiber for animation loop
    - Lerp (linear interpolate) between old and new positions
    - Smooth rotation changes for heading

    Animation approach:
    - Store target position from props
    - In useFrame, lerp current position toward target
    - Use lerp factor of 0.1 for smooth movement
    - Same for rotation

    This creates smooth movement rather than jumping when data updates.
  </action>
  <verify>
    <automated>cd /Users/laurent.prat/Documents/lpdev/databricks_airport_digital_twin/app/frontend && npx tsc --noEmit</automated>
  </verify>
  <done>Aircraft positions update smoothly</done>
</task>

</tasks>

<verification>
3D visualization complete:
1. Aircraft3D renders at correct lat/lon positions
2. Aircraft heading shown via rotation
3. 2D/3D toggle switches views
4. Positions update smoothly with new data
5. Selection syncs between views
</verification>

<success_criteria>
- Aircraft appear at correct positions in 3D space
- Aircraft rotate based on heading
- Clicking aircraft in 3D selects it
- 2D/3D toggle button works
- Position changes animate smoothly
- TypeScript compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/04-3d-visualization/04-02-SUMMARY.md`
</output>
