---
phase: 02-backend-2d-viz
plan: 03
type: execute
wave: 2
depends_on:
  - "02-01"
  - "02-02"
files_modified:
  - app/frontend/src/components/FlightList/FlightList.tsx
  - app/frontend/src/components/FlightList/FlightRow.tsx
  - app/frontend/src/components/FlightDetail/FlightDetail.tsx
  - app/frontend/src/components/GateStatus/GateStatus.tsx
  - app/frontend/src/components/Header/Header.tsx
  - app/frontend/src/App.tsx
  - app/frontend/src/hooks/useWebSocket.ts
  - app/frontend/src/context/FlightContext.tsx
autonomous: true
requirements:
  - UI-01
  - UI-02
  - UI-03
  - UI-04

must_haves:
  truths:
    - "Flight list displays all flights with search functionality"
    - "Clicking flight in list highlights on map"
    - "Gate status shows occupied/available gates"
    - "WebSocket updates flights in real-time"
  artifacts:
    - path: "app/frontend/src/components/FlightList/FlightList.tsx"
      provides: "Sortable, searchable flight table"
      exports: ["FlightList"]
    - path: "app/frontend/src/components/GateStatus/GateStatus.tsx"
      provides: "Gate occupancy status panel"
      exports: ["GateStatus"]
    - path: "app/frontend/src/hooks/useWebSocket.ts"
      provides: "WebSocket connection hook"
      exports: ["useWebSocket"]
---

<objective>
Create the UI components for flight list, gate status, and real-time WebSocket updates.

Purpose: Complete the user interface with supporting panels and real-time data flow for a polished demo experience.

Output: Flight list with search/sort, gate status panel, WebSocket integration, and coordinated map selection.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-backend-2d-viz/02-RESEARCH.md
@.planning/phases/02-backend-2d-viz/02-01-SUMMARY.md
@.planning/phases/02-backend-2d-viz/02-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Flight context and WebSocket hook</name>
  <files>app/frontend/src/context/FlightContext.tsx, app/frontend/src/hooks/useWebSocket.ts</files>
  <action>
    Create app/frontend/src/context/ directory.

    Create app/frontend/src/context/FlightContext.tsx:
    - FlightContextType: flights, selectedFlight, setSelectedFlight, isLoading
    - FlightProvider component wrapping children
    - Uses useFlights() hook for data
    - Provides context to children
    - useFlightContext() hook for consumers

    Create app/frontend/src/hooks/useWebSocket.ts:
    - useWebSocket(url) hook
    - Manages WebSocket connection lifecycle
    - Reconnects on disconnect with exponential backoff
    - Returns { lastMessage, isConnected, send }
    - Parses JSON messages automatically
  </action>
  <verify>
    <automated>cd /Users/laurent.prat/Documents/lpdev/databricks_airport_digital_twin/app/frontend && test -f src/context/FlightContext.tsx && test -f src/hooks/useWebSocket.ts && echo "Context and hooks created"</automated>
  </verify>
  <done>Flight context and WebSocket hook created</done>
</task>

<task type="auto">
  <name>Task 2: Flight list component</name>
  <files>app/frontend/src/components/FlightList/FlightList.tsx, app/frontend/src/components/FlightList/FlightRow.tsx</files>
  <action>
    Create app/frontend/src/components/FlightList/ directory.

    Create app/frontend/src/components/FlightList/FlightRow.tsx:
    - Props: flight, isSelected, onClick
    - Display: callsign, origin, altitude, velocity, phase
    - Highlight if selected
    - Color coding for flight phase
    - Click handler to select flight

    Create app/frontend/src/components/FlightList/FlightList.tsx:
    - Uses useFlightContext() for data
    - Search input filtering by callsign or icao24
    - Sort dropdown: callsign, altitude, velocity
    - Scrollable list of FlightRow components
    - Shows count of displayed vs total flights
    - Loading skeleton while fetching
  </action>
  <verify>
    <automated>cd /Users/laurent.prat/Documents/lpdev/databricks_airport_digital_twin/app/frontend && test -f src/components/FlightList/FlightList.tsx && echo "FlightList created"</automated>
  </verify>
  <done>Flight list with search and sort created</done>
</task>

<task type="auto">
  <name>Task 3: Gate status and flight detail</name>
  <files>app/frontend/src/components/GateStatus/GateStatus.tsx, app/frontend/src/components/FlightDetail/FlightDetail.tsx</files>
  <action>
    Create app/frontend/src/components/GateStatus/ directory.
    Create app/frontend/src/components/FlightDetail/ directory.

    Create app/frontend/src/components/GateStatus/GateStatus.tsx:
    - Display gate grid (A1-A10, B1-B10)
    - Color: green=available, red=occupied, yellow=delayed
    - For now, randomly assign some gates as occupied (Phase 3 will use ML)
    - Tooltip showing assigned flight on hover

    Create app/frontend/src/components/FlightDetail/FlightDetail.tsx:
    - Props: flight (from selectedFlight context)
    - Shows when a flight is selected
    - Full details: icao24, callsign, position, altitude, velocity, heading
    - Flight phase badge with icon
    - Data source indicator (opensky/synthetic)
    - Placeholder for ML predictions (Phase 3)
    - Close button to deselect
  </action>
  <verify>
    <automated>cd /Users/laurent.prat/Documents/lpdev/databricks_airport_digital_twin/app/frontend && test -f src/components/GateStatus/GateStatus.tsx && test -f src/components/FlightDetail/FlightDetail.tsx && echo "UI components created"</automated>
  </verify>
  <done>Gate status and flight detail panels created</done>
</task>

<task type="auto">
  <name>Task 4: App layout integration</name>
  <files>app/frontend/src/components/Header/Header.tsx, app/frontend/src/App.tsx</files>
  <action>
    Create app/frontend/src/components/Header/ directory.

    Create app/frontend/src/components/Header/Header.tsx:
    - App title: "Airport Digital Twin"
    - Connection status indicator (WebSocket connected/disconnected)
    - Last update timestamp
    - Flight count badge

    Update app/frontend/src/App.tsx:
    - Wrap in FlightProvider
    - Layout: Header at top, 3-column layout below
    - Left sidebar: FlightList (scrollable)
    - Center: AirportMap (main area)
    - Right sidebar: FlightDetail (when selected) + GateStatus
    - Responsive: sidebars collapse on smaller screens
  </action>
  <verify>
    <automated>cd /Users/laurent.prat/Documents/lpdev/databricks_airport_digital_twin/app/frontend && grep -q "FlightProvider" src/App.tsx && echo "App layout integrated"</automated>
  </verify>
  <done>Full app layout with all panels integrated</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Complete Phase 2 Backend API + 2D Visualization:
    1. FastAPI backend with REST and WebSocket endpoints
    2. React frontend with Vite + TypeScript
    3. Leaflet 2D map with airport overlay
    4. Flight markers with rotation and popups
    5. Flight list with search and sort
    6. Gate status panel
    7. Real-time WebSocket updates
  </what-built>
  <how-to-verify>
    **Local verification:**
    1. Start backend:
       ```bash
       cd /Users/laurent.prat/Documents/lpdev/databricks_airport_digital_twin
       pip install -r requirements.txt
       uvicorn app.backend.main:app --reload --port 8000
       ```
       Expected: Server starts on http://localhost:8000

    2. In another terminal, start frontend:
       ```bash
       cd /Users/laurent.prat/Documents/lpdev/databricks_airport_digital_twin/app/frontend
       npm install && npm run dev
       ```
       Expected: Vite dev server on http://localhost:5173

    3. Open browser to http://localhost:5173
       Expected:
       - Map displays with airport overlay
       - Flight markers appear and rotate
       - Flight list shows on left
       - Clicking flight highlights it

    4. Run backend tests:
       ```bash
       pytest tests/test_backend.py -v
       ```
       Expected: All tests pass

    **Success indicators:**
    - [ ] Backend /health returns 200
    - [ ] Frontend builds without TypeScript errors
    - [ ] Map shows airport runways/terminals
    - [ ] Flights appear on map with correct positions
    - [ ] Flight list filters when searching
  </how-to-verify>
  <resume-signal>Type "approved" if the app works correctly, or describe issues</resume-signal>
</task>

</tasks>

<verification>
UI components complete:
1. Flight list with search and sort
2. Gate status panel with color coding
3. Flight detail panel with full info
4. WebSocket integration for real-time updates
5. Coordinated selection between list and map
</verification>

<success_criteria>
- Flight list filters flights when searching
- Clicking flight in list highlights on map
- Gate status shows color-coded grid
- WebSocket reconnects automatically
- App layout is responsive and polished
</success_criteria>

<output>
After completion, create `.planning/phases/02-backend-2d-viz/02-03-SUMMARY.md`
</output>
