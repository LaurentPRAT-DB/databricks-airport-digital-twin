/**
 * Brand configuration — re-exports from brands/ directory.
 * The active brand is selected by VITE_BRAND env var (default: 'databricks').
 */

export { brand, type BrandConfig } from '../../brands';
